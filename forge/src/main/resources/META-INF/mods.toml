modLoader="javafml"
loaderVersion="[41,)"
license="Apache-2.0"
[[mods]]
modId="wardenlootforge"
version="1.0.9-1.19.x"
displayName="Warden Loot"
logoFile="wardenloot_icon.png"
authors="nu11une"
description='''
A configurable fabric mod that adds very powerful (and very hard to get) weapons and armor, themed around the Warden and the Ancient City
'''
[[dependencies.wardenlootforge]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[41,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.wardenlootforge]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.19,1.20)"
    ordering="NONE"
    side="BOTH"

[[dependencies.wardenlootforge]]
    modId="moremcmeta"
    mandatory=false
    versionRange="[1.19-3.0.3,)"
    ordering="NONE"
    side="CLIENT"